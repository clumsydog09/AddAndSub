#include <stdio.h>
int main() {
    int a,b;
    printf("Enter two numbers:\n");
    scanf("%d%d",&a,&b);
    printf("Sum of two numbers is: %d\n" ,(a+b));
    printf("The product of two numbers is %d", a*b);
    return 0;
}